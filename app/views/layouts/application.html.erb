<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Lynding" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Google Fonts - IBM Plex %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600&family=IBM+Plex+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 antialiased">
    <% if authenticated? %>
      <nav class="bg-white border-b border-gray-200" data-controller="mobile-menu">
        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
          <div class="flex h-16 justify-between">
            <div class="flex">
              <div class="flex flex-shrink-0 items-center">
                <%= link_to root_path, class: "text-xl font-bold text-gray-900 hover:text-gray-700 transition-colors" do %>
                  Lynding
                <% end %>
              </div>
              <div class="hidden sm:ml-8 sm:flex sm:space-x-8">
                <%= link_to "Dashboard", root_path, class: "inline-flex items-center border-b-2 #{'border-indigo-600 text-indigo-600' if current_page?(root_path)} px-1 pt-1 text-sm font-medium transition-colors #{'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-900' unless current_page?(root_path)}" %>
                <%= link_to "Loans", loans_path, class: "inline-flex items-center border-b-2 #{'border-indigo-600 text-indigo-600' if current_page?(loans_path)} px-1 pt-1 text-sm font-medium transition-colors #{'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-900' unless current_page?(loans_path)}" %>
                <%= link_to "Borrowers", borrowers_path, class: "inline-flex items-center border-b-2 #{'border-indigo-600 text-indigo-600' if current_page?(borrowers_path)} px-1 pt-1 text-sm font-medium transition-colors #{'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-900' unless current_page?(borrowers_path)}" %>
                <%= link_to "Agents", referral_agents_path, class: "inline-flex items-center border-b-2 #{'border-indigo-600 text-indigo-600' if current_page?(referral_agents_path)} px-1 pt-1 text-sm font-medium transition-colors #{'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-900' unless current_page?(referral_agents_path)}" %>
                <%= link_to "Payments", payments_path, class: "inline-flex items-center border-b-2 #{'border-indigo-600 text-indigo-600' if current_page?(payments_path)} px-1 pt-1 text-sm font-medium transition-colors #{'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-900' unless current_page?(payments_path)}" %>
              </div>
            </div>
            <div class="flex items-center">
              <div class="hidden sm:flex sm:items-center sm:space-x-4">
                <span class="text-sm text-gray-700">
                  <%= Current.user.email_address %>
                </span>
                <%= button_to "Sign out", session_path, method: :delete, class: "inline-flex items-center rounded-md bg-indigo-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600 transition-colors" %>
              </div>
              <!-- Mobile menu button -->
              <div class="flex items-center sm:hidden">
                <button type="button" data-action="click->mobile-menu#toggle" class="inline-flex items-center justify-center rounded-md p-2 text-gray-700 hover:bg-gray-100 hover:text-gray-900 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-indigo-500" aria-expanded="false">
                  <span class="sr-only">Open main menu</span>
                  <!-- Icon when menu is closed -->
                  <svg class="block h-6 w-6" data-mobile-menu-target="menuIcon" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
                  </svg>
                  <!-- Icon when menu is open -->
                  <svg class="hidden h-6 w-6" data-mobile-menu-target="closeIcon" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
                  </svg>
                </button>
              </div>
            </div>
          </div>
        </div>

        <!-- Mobile menu -->
        <div class="hidden sm:hidden" data-mobile-menu-target="menu">
          <div class="space-y-1 pb-3 pt-2">
            <%= link_to "Dashboard", root_path, class: "block border-l-4 #{'border-indigo-600 bg-indigo-50 text-indigo-700' if current_page?(root_path)} py-2 pl-3 pr-4 text-base font-medium transition-colors #{'border-transparent text-gray-600 hover:bg-gray-50 hover:border-gray-300 hover:text-gray-900' unless current_page?(root_path)}" %>
            <%= link_to "Loans", loans_path, class: "block border-l-4 #{'border-indigo-600 bg-indigo-50 text-indigo-700' if current_page?(loans_path)} py-2 pl-3 pr-4 text-base font-medium transition-colors #{'border-transparent text-gray-600 hover:bg-gray-50 hover:border-gray-300 hover:text-gray-900' unless current_page?(loans_path)}" %>
            <%= link_to "Borrowers", borrowers_path, class: "block border-l-4 #{'border-indigo-600 bg-indigo-50 text-indigo-700' if current_page?(borrowers_path)} py-2 pl-3 pr-4 text-base font-medium transition-colors #{'border-transparent text-gray-600 hover:bg-gray-50 hover:border-gray-300 hover:text-gray-900' unless current_page?(borrowers_path)}" %>
            <%= link_to "Agents", referral_agents_path, class: "block border-l-4 #{'border-indigo-600 bg-indigo-50 text-indigo-700' if current_page?(referral_agents_path)} py-2 pl-3 pr-4 text-base font-medium transition-colors #{'border-transparent text-gray-600 hover:bg-gray-50 hover:border-gray-300 hover:text-gray-900' unless current_page?(referral_agents_path)}" %>
            <%= link_to "Payments", payments_path, class: "block border-l-4 #{'border-indigo-600 bg-indigo-50 text-indigo-700' if current_page?(payments_path)} py-2 pl-3 pr-4 text-base font-medium transition-colors #{'border-transparent text-gray-600 hover:bg-gray-50 hover:border-gray-300 hover:text-gray-900' unless current_page?(payments_path)}" %>
          </div>
          <div class="border-t border-gray-200 pb-3 pt-4">
            <div class="flex items-center px-4">
              <div class="text-sm font-medium text-gray-800"><%= Current.user.email_address %></div>
            </div>
            <div class="mt-3 px-2">
              <%= button_to "Sign out", session_path, method: :delete, class: "block w-full rounded-md bg-indigo-600 px-3 py-2 text-center text-base font-medium text-white shadow-sm hover:bg-indigo-500 transition-colors" %>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <main class="<%= authenticated? ? 'min-h-screen py-8' : 'min-h-screen' %>">
      <%= yield %>
    </main>
  </body>
</html>
